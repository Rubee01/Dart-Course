void main(List<String> args) {
   //const: unchangeable, immutable variable
   //define a const variable
   const integer = '7000';
   print(integer);

   //modification cannot be applied to the internal value of the constant
   //invalid code
   //const myList = [1,2,3];
   //myList.add(5);

   //assign new value is also not allowed
   //invalid code
   //integer = '6789'
}