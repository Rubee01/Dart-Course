void main(List<String> args) {
  //most of the type Dart will automatically infer the data type for users
  const int myHeight = 170;
  const yourHeight = 150; //same thing, use this
  print(myHeight);
  print(yourHeight);
}