void main(List<String> args) {
  //compound operator: 2 operator in 1
  var myAge = 20;
  print(myAge ~/= 2);   //compute and assign the new value back to the value
                        //applied to every arithmetic, bitwise operators
  print(myAge *= 2);
  print(myAge += 2);
}